/** @type {import('next').NextConfig} */
const nextConfig = {
    // Ensure we can handle any potential transpilation needs if strict
    transpilePackages: ['animated-emoji-4v']
};

export default nextConfig;
